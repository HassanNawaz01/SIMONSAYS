{
  "name": "files",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "compile": "hardhat compile",
    "deploy:score": "node scripts/deploy.js",
    "deploy:escrow": "node scripts/deploy-escrow.js",
    "stake:allow": "node scripts/set-allowed-stake.js",
    "settle:recovery": "node scripts/settle-recovery.js",
    "test": "hardhat compile && node --test --test-concurrency=1",
    "test:server": "node --test --test-concurrency=1 test/server.test.js",
    "test:escrow": "hardhat compile && node --test --test-concurrency=1 test/escrow.test.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "hardhat": "^3.9.1"
  },
  "dependencies": {
    "ethers": "^6.17.0",
    "ws": "^8.21.0"
  }
}
